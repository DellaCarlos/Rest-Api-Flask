# Flask REST API - Users

A simple RESTful API built with **Flask**, **Flask-RESTful**, and **Flask-SQLAlchemy** for user management.  
The application uses **SQLite** as its database.

---

# Database

The SQLite database is created automatically when the application runs for the first time.
User Model.
